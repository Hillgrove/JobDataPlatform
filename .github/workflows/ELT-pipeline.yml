name: ELT Pipeline

on:
  schedule:
    - cron: '0 1 * * *' # hver nat at 03:00 dansk tid
  workflow_dispatch:

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Check kode ud
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore afhængigheder
        run: dotnet restore CLI/CLI.csproj

      - name: Opret secrets-mappe og gem GCS nøgle
        run: |
          mkdir -p CLI/secrets
          echo "${{ secrets.GCP_SA_KEY }}" > CLI/secrets/gcs-key.json

      - name: Kør pipeline
        run: dotnet run --project CLI/CLI.csproj